import React, { useState } from 'react';

const Role = () => {

    const [role, setRole] = useState("admin"); // change করে "user" করলেই user mode

    return (
        <div>
            
        </div>
    );
};

export default Role;